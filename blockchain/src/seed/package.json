{
  "engines": {
    "composer": "^0.20.5"
  },
  "name": "seed",
  "version": "0.0.1",
  "description": "seed PoC",
  "scripts": {
    "prepublish": "mkdirp ./dist && composer archive create --sourceType dir --sourceName . -a ./dist/seed.bna",
    "test": "nyc mocha -t 0 test/*.js"
  },
  "keywords": [
    "composer",
    "composer-network"
  ],
  "author": "tanguy",
  "email": "tanguy@flexper.fr",
  "license": "Apache-2.0",
  "devDependencies": {
    "composer-admin": "^0.20.5",
    "composer-cli": "^0.20.5",
    "composer-client": "^0.20.5",
    "composer-common": "^0.20.5",
    "composer-connector-embedded": "^0.20.5",
    "chai": "latest",
    "chai-as-promised": "latest",
    "eslint": "latest",
    "nyc": "latest",
    "mkdirp": "latest",
    "mocha": "latest"
  }
}
